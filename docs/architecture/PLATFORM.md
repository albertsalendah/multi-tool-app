# Platform

The Platform is responsible for application lifecycle, orchestration, dependency management, shared services, configuration and security.

It does not contain tool-specific business logic.
